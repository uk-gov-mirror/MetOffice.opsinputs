time window:
  begin: 2018-01-01T00:00:00Z
  end: 2018-01-01T02:00:00Z

observations:
  - obs space:
      name: AMSUB
      obsdatain:
        engine:
          type: H5File
          obsfile: Data/dummy.hc4
      simulated variables: [dummy]
    geovals:
      filename: Data/024_VarField_modelorog_geoval.nc4
    obs filters:
    # Double all observation errors: we want to check if error changes made by filters are
    # propagated to VarObs files
    - filter: BlackList
      action:
        name: inflate error
        inflation factor: 2.0
    # Set the flag of observations with missing values to "pass": we want to check if these
    # values are encoded correctly in the VarObsFile.
    - filter: Reset Flags to Pass
      flags_to_reset: [10, 15]  # missing, Hfailed
    # Reject observation 3: we want to check if it is omitted from the VarObs file, as expected.
    - filter: Domain Check
      where:
      - variable:
          name: MetaData/latitude
        minvalue: 0.0
    - filter: VarObs Writer
      reject_obs_with_any_variable_failing_qc: true
      namelist_directory: testinput/VarObsWriterNamelists_024_VarField_modelorog
      general_mode: debug
    - filter: VarObs Checker
      expected_main_table_columns:
      - # batch 1
        - ["7.10"]       # column 1
        - ["**********"] # column 2 (the asterisks represent a missing float)
        - ["7.40"]       # column 3
    HofX: ObsValue # just a placeholder -- not used, but needed to force calls to postFilter.
    benchmarkFlag: 1000 # just to keep the ObsFilters test happy
    flaggedBenchmark: 0
